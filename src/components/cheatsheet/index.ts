/**
 * The printable reference's own pieces: the chrome that never prints, and the
 * colour a block takes from the level that taught it.
 */

export type { CheatsheetHeaderProps } from './CheatsheetHeader.tsx';
export { CheatsheetHeader } from './CheatsheetHeader.tsx';
export type { LevelLegendEntry } from './levels.ts';
export {
  LEVEL_LEGEND,
  colouredSections,
  levelColour,
  printedLegend,
} from './levels.ts';
